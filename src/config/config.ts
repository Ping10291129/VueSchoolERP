export default {
  /**
   * @description 请求配置
   */
  request: {
    withCredentials: true,
    // 设置前置URL
    baseURL: "",
    // 超时时间
    timeout: 10000,
    // 跨域是否携带 token
    withToken: true
  }
};
